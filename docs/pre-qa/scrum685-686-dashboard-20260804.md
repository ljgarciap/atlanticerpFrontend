# Pre-QA — SCRUM-685 / SCRUM-686 (Dashboard CRM)

**Fecha:** 2026-08-04
**Entorno:** local (Docker `infra-*` + Vite dev server `localhost:5173`)
**Usuario:** daniela@atlantic.com.pa (Gerencia Total)
**Herramienta:** Playwright CLI, spec descartable `e2e/scratch-scrum685-686-dashboard-20260804.spec.ts` (no promovido a permanente — hallazgo es cosmético/CSS, no gate de flujo/permiso/estado, ver criterio en `pre-qa.md`)

## Criterios de aceptación ejercitados

### SCRUM-685 / REQ-605 (tarjetas de conteo + Pipeline Activo + Cerrado Ganado)
- Escenario 1 (conteo por etapa exacto) — camino feliz, ya validado en el batch original
  (2026-07-31) y no tocado por este fix. Re-confirmado con datos actuales (Lead 1, Diseño 1,
  Aprobado 1, resto 0) y con dataset inflado (Lead 26, Aprobado 2).
- Fix puntual bajo prueba: ausencia de las 2 tarjetas grandes duplicadas — confirmado ausente en
  ambos datasets (baseline y stress), conteo de apariciones del texto "Pipeline activo" = 1 y
  "Cerrado (Ganado)" = 1 en toda la página (antes del fix hubiera dado 2).

### SCRUM-686 / REQ-606 (gráfico de barras)
- Escenario 1 (orden fijo) — confirmado en ambos datasets: Lead, Diseño, Cotización, Propuesta,
  Aprobado, Perdido, siempre en ese orden sin importar el conteo de cada uno.
- Escenario 2 (altura proporcional) — confirmado: en el dataset de stress, Lead=26 ocupa el 100%
  de la altura disponible, Aprobado=2 una fracción visualmente proporcional.
- Escenario 3 (etapa en 0 con barra mínima visible + "0" sobre ella) — confirmado en ambos
  datasets: Cotización/Propuesta/Perdido en 0 muestran una barra mínima visible (mecanismo
  `minBarLength: 4` de Chart.js) y el número "0" completamente visible sobre la barra, sin quedar
  pegado ni cortado contra el eje X.
- Fix puntual bajo prueba (RN3 — número no recortado contra el borde superior): confirmado en dos
  escenarios de ruptura intencional, ver abajo.

## Escenarios de ruptura intentados (más allá del camino feliz)

| # | Escenario de ruptura | Resultado |
|---|---|---|
| 1 | Barra al 100% de la altura del área de trazado (dataset baseline real: Lead/Diseño/Aprobado todos en 1, que es también el máximo del eje) — el caso técnicamente más exigente para el padding, ya que la barra toca el borde superior del área de trazado | ✅ El número queda completamente visible con margen blanco arriba, no recortado |
| 2 | Disparidad extrema de conteos (dataset inyectado a propósito vía DB: Lead=26 vs Diseño=1, Aprobado=2, resto=0) — reproduce el mismo tipo de escenario que el bug original reportado por Daniela (captura adjunta 11664, "20" cortado a la mitad) | ✅ El número "26" sobre la barra más alta queda completamente visible, sin recorte — comparado directamente contra la captura del bug original, confirma que el fix (`layout.padding.top: 20`) resuelve el caso real reportado |
| 3 | Monto muy grande en "Cerrado (Ganado)" ($9,876,650.21, vs $107 real) | ✅ Formatea correctamente con comas (`$9,876,650`), sin overflow ni wrap en la tarjeta chica del grid de 8 |
| 4 | Recarga de página a mitad de carga (`page.reload()` a los ~150ms de haber navegado a `/crm/dashboard`, mientras el query de TanStack aún no resolvía) | ✅ Sin texto de error visible tras la recarga; el dashboard vuelve a cargar limpio (loading → datos), sin quedar en un estado roto o a medio pintar |
| 5 | Usuario sin rol Gerencia intentando acceder a `/crm/dashboard` por URL directa (neil.quiel@atlantic.com.pa, rol vendedor/diseñador) | ✅ Redirigido a `/ventas-diseno/home` — el gate `RequireRole roles={['management','superadmin']}` sigue bloqueando correctamente; no es parte del alcance de estos 2 tickets, se verificó como chequeo de regresión porque ambos fixes tocan el mismo archivo (`DashboardPage.tsx`) |
| 6 | Datos en 0 en TODAS las etapas simultáneamente | No ejercitado — el fixture local no permite vaciar todas las etapas sin afectar otros tests/datos compartidos del stack local (mismo Postgres que otras specs de Pre-QA, ver gotcha `feedback_local_test_db_no_es_fresh`). Cubierto parcialmente por el escenario 3 de arriba (3 de 6 etapas en 0 simultáneamente, incluyendo la etapa vecina a la barra más alta) — no se considera un hueco bloqueante porque el mecanismo (`minBarLength` fijo + plugin que lee `chart.data.datasets[0].data[i]` por índice) no tiene ninguna rama de código que dependa de que "todas" estén en 0 a la vez |

## Datos de prueba — nota de higiene

Para los escenarios 2 y 3 se insertaron 25 `pipeline_cards` en etapa `lead` (sin monto) + 1 en
`approved` con `amount = 9876543.21`, vía `sales_projects`/`pipeline_cards` con prefijo
`PreQA StressTest*`, reutilizando `master_client_id`/`sub_client_id`/`owner_id` ya existentes en el
entorno local. **Se eliminaron al finalizar la verificación** (`DELETE` de las 26 filas de
`sales_projects` con ese prefijo + sus `pipeline_cards` asociadas) — el stack local quedó en el
mismo estado que antes de esta sesión (`lead:1, design:1, approved:1`, confirmado por conteo
posterior a la limpieza).

## Lo que sí funciona
- Conteo exacto por etapa (RN1 REQ-605), en datos reales y con volumen inflado
- Suma de Pipeline Activo excluyendo Aprobado/Perdido, y de Cerrado (Ganado) incluyendo solo
  Aprobado (RN2/RN3 REQ-605) — no tocado por este fix, re-confirmado igualmente
- Formato de montos grandes sin overflow visual
- Orden fijo de las 6 barras (RN1 REQ-606) independiente del conteo
- Altura proporcional (RN2 REQ-606) con disparidad extrema de datos
- Barra mínima visible + "0" visible en etapas sin proyectos (RN4 REQ-606)
- Número sobre cada barra sin recorte (RN3 REQ-606), incluido el caso que reprodujo el bug
  original reportado por Daniela
- Gate de rol de la ruta `/crm/dashboard` sin regresión
- Recarga a mitad de carga sin estado roto
- Ausencia de las 2 tarjetas grandes duplicadas (cambio de alcance de SCRUM-685), consistente en
  datos reales y con volumen inflado

## Hallazgos
Ninguno. Pasada limpia en la primera vuelta — no hizo falta loop de corrección.

## Veredicto

**Pasada limpia. SCRUM-685 y SCRUM-686 listos para pasar a QA.**
