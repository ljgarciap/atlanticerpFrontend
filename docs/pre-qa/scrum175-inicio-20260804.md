# Pre-QA — SCRUM-175 (REQ-111): Inicio de Compras + `getHomeRoute()`

**Fecha:** 2026-08-04
**Entorno:** local (Docker :8090 + Vite dev server :5173), rama `dev`, working tree local, fix ya mergeado (commit `78cd403 merge: consolidar fix/scrum-175-inicio-layout a dev local`)
**Smoke test permanente:** `e2e/preqa-scrum175-inicio-compras-routing-20260804.spec.ts` (6/6 verde) — promovido a `e2e/` por ser un gate de routing que ya se rompió una vez (loop infinito), regla del CLAUDE.md del proyecto.
**Unit test de soporte:** `src/lib/homeRoute.test.ts` (7/7 verde, pre-existente, cubre `getHomeRoute()` de forma determinística sin navegador).

## Foco principal — CRÍTICO original: loop infinito para Yirena

**Antes del fix:** `gerencia2@atlantic.com.pa` (Yirena, `lider_compras`) quedaba en pantalla en blanco al hacer login — `App.tsx` tenía `FALLBACK_ROUTE = '/ventas-diseno/home'` fijo; Yirena solo tiene visibilidad de módulo para `compras`, así que `RequirePermission` de esa ruta la rebotaba de vuelta al mismo fallback, en loop.

**Confirmado en vivo (no solo la URL, sino contenido real renderizado):**
- Login real con `gerencia2@atlantic.com.pa` / password = mismo email — funcionó al primer intento, sin necesidad de fallback de password.
- Aterriza directo en `/compras/inicio` (sin pasar visiblemente por `/ventas-diseno/home`).
- Contenido visible y funcional: saludo "Hola, Yirena" (o nombre real), "Resumen del mes", "Mi calendario", "Pendientes", "Órdenes críticas" — no una pantalla en blanco ni un spinner colgado.
- Repetido logout → login dos veces seguidas (test #2) — mismo resultado, descarta que fuera un fluke de caché/estado de sesión previo.

## Regresión — otros perfiles del roster real

| Usuario | Rol | Módulo esperado (por prioridad ventas_diseno > compras > bodega > servicios > admin_contab > gerencia > operaciones) | Resultado observado |
|---|---|---|---|
| `gerencia2@atlantic.com.pa` (Yirena) | `lider_compras`, `compras.view=true` + extra `bodega.read` | `/compras/inicio` (compras antes que bodega) | ✅ `/compras/inicio` |
| `daniela@atlantic.com.pa` (Daniela Amaya) | Gerencia Total, acceso a todos los módulos | `/ventas-diseno/home` (primer módulo del catálogo) | ✅ `/ventas-diseno/home` |
| `almacen@atlantic.com.pa` (Esteban Cárdenas) | `lider_bodega`, sin compras/ventas_diseno | `/bodega/home` | ✅ `/bodega/home` |
| `asistente@atlantic.com.pa` (Yaneth Pineda) | `asistente_administrativa`, Nivel 1 Read Only — perfil más angosto del roster real | Algún módulo con `view=true` o `/settings` | ✅ `/admin-contab` (tiene `admin_contab.view=true`) — sin loop, sin blanco, sin quedar en `/login` |

**Nota sobre el fallback universal (`/settings`):** ningún usuario del roster real de 32 personas (dev/test) tiene CERO módulos de negocio asignados — el perfil más angosto (Yaneth) igual aterriza en un módulo real (`/admin-contab`, placeholder "próximamente" pero pantalla real, no loop). El caso exacto de `modules` completamente vacío está cubierto de forma determinística en `homeRoute.test.ts` ("sin ningún módulo visible, cae al fallback universal → `/settings`") — no se pudo, ni hacía falta, forzarlo con una cuenta real, porque no existe ese perfil hoy en el sistema.

## Catch-all — ruta vieja eliminada

Con Yirena ya logueada en `/compras/inicio`, navegación manual a `/dashboard` (ruta eliminada en SCRUM-711) → resuelve a `/compras/inicio` de nuevo (su home real), contenido visible, sin loop ni pantalla en blanco.

## Camino de ruptura adicional intentado
- Doble logout/login consecutivo → mismo resultado ambas veces (test #2).
- Navegación directa por URL a ruta legado sin pasar por links de la UI (test catch-all).
- Perfil de menor acceso disponible en el roster real, no un superusuario (Yaneth en vez de otro superadmin).

## CRÍTICO
Ninguno.

## MEDIO
Ninguno.

## Lo que sí funciona
- Fix de `getHomeRoute()` resuelve correctamente el home de 4 perfiles reales distintos, respetando el orden de prioridad `ventas_diseno > compras > bodega > servicios > admin_contab > gerencia > operaciones`.
- `superadmin.all` y usuarios con múltiples módulos siguen resolviendo al primero del catálogo (Daniela → Ventas & Diseño), sin regresión.
- Catch-all (`*` → `CatchAllRedirect`) usa el mismo `getHomeRoute()`, no un fallback fijo — rutas legado no generan loop.
- Logout/login repetido no revela ningún estado de caché corrupto.
- Layout de "Resumen del mes"/"Mi calendario"/"Pendientes"/"Órdenes críticas" verificado por coordenadas reales (`boundingBox()`), no solo por inspección visual — confirma el fix de `lg:col-span-2` sin regresión.

## Resultado
**PASADA LIMPIA.** Sin hallazgos bloqueantes tras el checklist completo (routing + layout + regresión + camino de ruptura). SCRUM-175 puede transicionar a `QA`.
